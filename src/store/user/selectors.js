export const selectToken = (reduxState) => reduxState.user.token;

export const selectUser = (reduxState) => reduxState.user.profile;

export const selectMyProfile = (reduxState) => reduxState.user.myProfile;
